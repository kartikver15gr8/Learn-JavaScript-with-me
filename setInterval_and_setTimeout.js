const sum = (a, b) => {
  console.log("The sum of a and b is : " + (a + b));
  a + b;
}

// setInterval(sum, 5000, 34, 53);

const msg = () => {
  console.log("Hey there how's it all going!");
}

// setTimeout 

setTimeout(msg, 5000);
console.log(msg);
